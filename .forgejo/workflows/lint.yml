on: [ push ]

jobs:
  lint:
    runs-on: docker
    steps:
      - uses: https://data.forgejo.org/actions/checkout@v3

      - name: "Run PyLint"
        uses: https://forgejo.van-de-weerd.net/parcifal/common/pylint@master
        with:
          sources: ris test
