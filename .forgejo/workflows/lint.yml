on: [ push ]

jobs:
  lint:
    runs-on: docker
    steps:
      - name: "Run PyLint"
        uses: https://forgejo.van-de-weerd.net/parcifal/common/pylint@master
        with:
          sources: ris test
